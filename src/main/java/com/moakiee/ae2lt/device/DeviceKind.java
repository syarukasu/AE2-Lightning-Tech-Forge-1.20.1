package com.moakiee.ae2lt.device;

// Identity for top-level overload devices. Modules declare which kinds they accept.
public enum DeviceKind {
    RAILGUN,
    OVERLOAD_HELMET,
    OVERLOAD_CHESTPLATE,
    OVERLOAD_LEGGINGS,
    OVERLOAD_BOOTS
}
